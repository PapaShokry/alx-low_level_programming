#include <stdio.h>

/**
 * Main - Entry point
 * Description: use printf to print a text
 * Return: 0
 */

int main(void)
{
printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
