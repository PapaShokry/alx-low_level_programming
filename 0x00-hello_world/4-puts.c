#include <stdio.h>
/*
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 * Prints a text in stdout
 */
int main(void)
{
char myStr[] = "\"Programming is like building a multilingual puzzle\n";
puts(myStr);
return (0);
}
