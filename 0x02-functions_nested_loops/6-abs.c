#include "main.h"

/**
 * _abs - entry to function
 * @n: the integer received
 * Description: Returns the absolute value of an integer
 * Return: int
 */
int _abs(int n)
{
return (n < 0 ? -n : n);
}
